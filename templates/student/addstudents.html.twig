{% extends 'base.html.twig' %}

{% block title %}Teachermarks - Ajout d'élève{% endblock %}

{% block body %}

<div class="container-fluid">
    <div class="bg-secondary bg-opacity-25 m-lg-5 p-3 rounded-3">

        <h2>Ajouter un élève</h2>

        {{form_start(form)}}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success rounded-3">Valider
        </button>
        {{form_end(form)}}
        
    </div>
</div>


{% endblock %}
